# JTAG pyUVM Testbench Python 依赖
#
# 安装方式：
#   pip install -r requirements.txt
#
# 前提：需要系统已安装 iverilog（icarus verilog）
#   Linux:   sudo apt install iverilog
#   macOS:   brew install icarus-verilog

# UVM Python 实现（含 cocotb 依赖）
pyuvm>=2.9.0

# cocotb 仿真框架（pyuvm 依赖，显式列出确保版本兼容）
cocotb>=1.8.0
